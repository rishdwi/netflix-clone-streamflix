version: "3.8"
services:
  mongo:
    image: mongo:7
    container_name: netflix-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: app_db
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data:
